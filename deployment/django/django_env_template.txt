File name: specifed in the docker-compose, by default "django_prod.env"

DEBUG=bool
SECRET_KEY=secret_key
DB_NAME=str
DB_USER=str
DB_PASSWORD=str
DB_HOST=str(docker container name)
DB_PORT=int(typical 5432)